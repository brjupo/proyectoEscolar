

const Entrada = (props) => {
  return <input className="entrada" onChange={props.manejarCambio}/>;
};

// Entrada.propTypes = {
//   texto: PropTypes.string.isRequired,
//   handleClick: PropTypes.func.isRequired,
// };

export default Entrada;
