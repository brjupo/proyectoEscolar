

const Resultado = (props) => {
  return <div className="resultado">{props.resultado}</div>;
};

// Resultado.propTypes = {
//   texto: PropTypes.string.isRequired,
//   handleClick: PropTypes.func.isRequired,
// };

export default Resultado;
